# 部署计划模板

用于一次具体部署前填写。

## 目标

- 部署环境：
- 目标提交：
- 本次变更类型：安全 / 功能 / bugfix / 文档 / 混合
- 是否用户可见：
- 先验环境：腾讯云 / 其他 / 无
- 是否允许跳过分阶段验收：
- 是否包含 Nginx 变更：
- 是否包含 `.env` 变更：
- 是否包含前端 JS/CSS 源码变更：
- 是否包含运行数据同步策略变更：

## 部署前检查

- [ ] 本地验证命令已通过
- [ ] JS/CSS dist 已重建并提交
- [ ] `.env.example` 已更新
- [ ] 用户已确认需要手动同步的真实 `.env` 项
- [ ] 用户可见的多环境更新已计划先部署先验环境，等待用户手动确认后再同步后续环境
- [ ] 如涉及运行数据同步，已核对 project profile、每日检查、运行状态、安全基线和 AGENTS 是否需要同步更新
- [ ] 已说明本次部署的影响和回滚方式

## 部署步骤

```bash
# 阶段 1：先验环境，例如腾讯云
TODO

# 阶段 2：用户手动确认先验环境通过后，才同步后续环境，例如阿里云
TODO
```

## 通用烟测

```bash
TODO
```

## 本次任务特定验收

不要填入无关安全检查。只写本次任务真正需要证明的事项。

- [ ] TODO
- [ ] 如涉及腾讯云到阿里云数据同步：新 cron/日志正常，旧 cron/进程已停用，稳定小文件 hash 一致，动态小数据延迟符合预期；分组同步下动态变化优先只触发 `groups=dynamic`

## 回滚或止损

- 代码回滚：
- 服务回滚：
- Nginx 回滚：
- 用户需要知道的影响：
